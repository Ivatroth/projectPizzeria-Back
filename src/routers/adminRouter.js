// Inicialido el enrutador de express
const { Router } = require('express')
const adminRouter = Router()
//const { getAlgo } = require('../handlers/adminHandlers')

// metodos CRUD
//adminRouter.get('/', getAlgo)

module.exports = adminRouter